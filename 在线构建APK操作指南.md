# 在线构建APK操作指南

> 使用 Codemagic 在线服务将 Flutter 项目构建为 Android APK，无需本地安装 Flutter 环境。

---

## 准备工作

### 1. 打包项目代码

将项目文件夹压缩为 ZIP 文件：

```
question_reminder_app/
├── android/
├── lib/
├── assets/
├── pubspec.yaml
├── codemagic.yaml
└── README.md
```

**压缩步骤：**
1. 进入项目文件夹
2. 全选所有文件和文件夹
3. 右键 → 发送到 → 压缩(zipped)文件夹
4. 命名为 `question_reminder_app.zip`

---

## 注册 Codemagic 账号

### 步骤 1：访问官网

打开浏览器访问：
**https://codemagic.io**

### 步骤 2：注册账号

1. 点击 **"Sign up"** 或 **"Get started"**
2. 选择注册方式：
   - GitHub 账号（推荐）
   - Google 账号
   - 邮箱注册

3. 完成注册后登录

---

## 上传项目并构建

### 步骤 1：创建新应用

1. 登录后点击 **"Add application"**（添加应用）
2. 选择 **"Other"**（其他）→ **"Flutter App"**（Flutter应用）

### 步骤 2：上传代码

**方式一：通过 GitHub（推荐）**

1. 将代码推送到 GitHub 仓库
2. 在 Codemagic 中连接 GitHub 账号
3. 选择对应的仓库

**方式二：直接上传（无需GitHub）**

1. 点击 **"Upload"** 或 **"Import repository"**
2. 选择刚才压缩的 `question_reminder_app.zip` 文件
3. 等待上传完成

### 步骤 3：配置构建设置

1. 进入应用设置页面
2. 在 **Workflows**（工作流）中选择 **"Android"**
3. 确认构建设置：
   - **Build format**: APK
   - **Mode**: Release
   - **Flutter version**: Stable

### 步骤 4：开始构建

1. 点击 **"Start new build"**（开始新构建）
2. 选择 **"Android"** 平台
3. 点击 **"Start build"**

### 步骤 5：等待构建完成

- 构建时间约 **5-15 分钟**
- 可以在页面查看实时日志
- 构建成功后会显示绿色勾号 ✅

---

## 下载 APK

### 步骤 1：获取构建产物

构建完成后：

1. 点击构建记录进入详情页
2. 找到 **Artifacts**（产物）部分
3. 点击 `app-release.apk` 下载

### 步骤 2：传输到手机

**方式一：直接下载到手机**
- 在手机上打开 Codemagic 网站
- 登录账号下载 APK

**方式二：电脑传输**
- 下载到电脑后，通过微信/QQ/数据线传到手机

---

## 安装 APK

### 步骤 1：允许安装未知应用

1. 打开手机 **设置**
2. 搜索 **"未知应用"** 或 **"安装未知应用"**
3. 找到浏览器/文件管理器，允许安装

### 步骤 2：安装应用

1. 在文件管理器中找到下载的 APK
2. 点击 APK 文件
3. 点击 **"安装"**
4. 等待安装完成

### 步骤 3：打开应用

安装完成后，桌面会出现 **"问题记录"** 图标，点击即可使用。

---

## 常见问题

### Q1: 构建失败怎么办？

**检查清单：**
- [ ] pubspec.yaml 中的依赖版本是否正确
- [ ] android/build.gradle 配置是否正确
- [ ] 代码中是否有语法错误

**查看日志：**
- 在 Codemagic 构建详情页查看错误日志
- 根据错误提示修改代码后重新构建

### Q2: 安装时提示"解析包时出现问题"

**解决方法：**
1. 确认 APK 文件完整下载
2. 检查手机 Android 版本是否 >= 5.0 (API 21)
3. 重新下载 APK

### Q3: 应用闪退怎么办？

**可能原因：**
- 缺少权限：首次打开时允许所有权限请求
- 存储空间不足：清理手机存储
- 兼容性问题：尝试在另一台手机上安装

---

## 替代方案

如果 Codemagic 不方便使用，还可以尝试：

### 方案 A：GitHub Actions（免费）

1. 将代码上传到 GitHub
2. 配置 GitHub Actions 工作流
3. 自动构建 APK

### 方案 B：找朋友帮忙

1. 将代码发给有 Flutter 环境的朋友
2. 朋友运行 `flutter build apk`
3. 将生成的 APK 发回给您

### 方案 C：使用电脑（最稳定）

1. 在电脑上安装 Flutter SDK
2. 连接手机，开启 USB 调试
3. 运行 `flutter run` 直接安装到手机

---

## 需要帮助？

如果在构建过程中遇到问题：

1. **查看构建日志** - Codemagic 会显示详细的错误信息
2. **检查代码** - 确保所有文件完整且正确
3. **搜索错误信息** - 将错误信息复制到搜索引擎查找解决方案

---

**祝您使用愉快！**
